import Radio from '@material-ui/core/Radio';
import styled from 'styled-components';

const QueryRadio = styled(Radio)(({ theme }) => ({
// width: 18,
// height: 8,
// padding: 0,

}));

export default QueryRadio;